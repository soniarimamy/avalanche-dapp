{
  "name": "avalanche-smart-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Avalanche DApp",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:fuji": "npx hardhat run scripts/deploy.cjs --network fuji",
    "deploy:local": "npx hardhat run scripts/deploy.cjs --network hardhat"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^7.0.0",
    "dotenv": "^16.0.3",
    "hardhat": "^3.4.3"
  }
}
